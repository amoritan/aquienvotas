import styled from 'styled-components'

const SectionTitle = styled.h2`
  font-size: 2em;
  font-weight: 700;
  color: #fefefe;
  margin: 1rem;
  small {
    font-size: 1em;
    font-weight: 600;
    margin-right: .25em;
    color: #0095d5;
  }
`

export default SectionTitle
